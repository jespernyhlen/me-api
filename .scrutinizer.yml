build:
  nodes:
    coverage:
      tests:
        override:
          - js-scrutinizer-run
          - npm test
            coverage:
              file: coverage/clover.xml
              format: clover